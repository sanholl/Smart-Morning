---
name: morning-briefing
description: 매일 아침 경제 뉴스를 자동으로 수집하고 요약하여 카카오톡으로 전송하는 스킬. "아침 브리핑", "경제 뉴스", "morning briefing" 요청에 사용.
triggers:
  - "아침 브리핑"
  - "경제 뉴스"
  - "morning briefing"
  - "모닝 브리핑"
---

# Morning Briefing

매일 아침 9시, 경제 뉴스 주요 소식을 요약해서 카카오톡으로 받는 스킬.

## 개요

이 스킬은 다음과 같은 작업을 자동으로 수행합니다:

1. **네이버 경제 뉴스** 수집 (최신 10개 기사)
2. **주요 소식만 요약** (Claude AI 활용)
3. **카카오톡으로 전송** (나에게 보내기 API)

## 실행 방법

### 1. 수동 실행

Claude Code에서 바로 실행:

```
/morning-briefing
```

또는 터미널에서:

```bash
cd /Users/sol/Desktop/sol/claude
claude skill run morning-briefing
```

### 2. 자동 실행 (스케줄링)

Mac의 cron을 사용하여 매일 아침 9시에 자동 실행:

```bash
# crontab 편집
crontab -e

# 아래 줄 추가 (매일 오전 9시)
0 9 * * * cd /Users/sol/Desktop/sol/claude && claude skill run morning-briefing
```

## 스킬 실행 흐름

### Step 1: 네이버 경제 뉴스 수집

네이버 뉴스 경제 섹션 RSS 피드에서 최신 뉴스를 수집합니다.

수집 소스:
- URL: `https://news.naver.com/main/list.naver?mode=LSD&mid=sec&sid1=101` (경제 섹션)
- RSS: `https://news.naver.com/mainRss.naver?sid1=101` (경제 RSS)

수집 항목:
- 기사 제목
- 기사 요약
- 기사 링크
- 발행 시간

### Step 2: 주요 소식 요약

Claude AI가 수집된 뉴스를 분석하여 요약합니다.

요약 기준:
- **핵심 3-5개 뉴스**만 선별
- 각 뉴스를 **2-3줄**로 간단 요약
- 주식/환율/금리 등 **주요 지표** 정리
- 읽기 쉬운 **불릿 포인트** 형식

출력 형태:
```
📰 오늘의 경제 뉴스 (2026-02-26)

📌 주요 소식
• [제목] 간단한 요약 내용
• [제목] 간단한 요약 내용

📊 주요 지표
• 코스피: 2,650 (+0.8%)
• 달러: 1,330원 (-5원)

🔗 전체 기사
• [링크1]
• [링크2]
```

### Step 3: 카카오톡 전송

Node.js 스크립트를 통해 카카오톡 "나에게 보내기" API로 전송합니다.

전송 방식:
```bash
node .claude/skills/morning-briefing/scripts/send-kakao.js
```

필요한 설정:
- 카카오 개발자 앱 등록
- REST API 키 발급
- Access Token 발급

## 사전 준비 사항

### 1. 카카오 개발자 설정

1. [카카오 개발자 사이트](https://developers.kakao.com/) 접속
2. 애플리케이션 생성
3. "내 애플리케이션 > 앱 설정 > 요약 정보"에서 **REST API 키** 복사
4. "내 애플리케이션 > 제품 설정 > 카카오 로그인" 활성화
5. Redirect URI 설정: `http://localhost:3000/callback`
6. "동의 항목"에서 "카카오톡 메시지 전송" 권한 설정

### 2. Access Token 발급

터미널에서 실행:

```bash
node .claude/skills/morning-briefing/scripts/get-kakao-token.js
```

발급된 토큰을 `.env` 파일에 저장:

```env
KAKAO_REST_API_KEY=your_rest_api_key
KAKAO_ACCESS_TOKEN=your_access_token
```

### 3. Node.js 패키지 설치

```bash
cd .claude/skills/morning-briefing
npm install axios dotenv
```

## 파일 구조

```
.claude/skills/morning-briefing/
├── SKILL.md                    # 이 파일
├── scripts/
│   ├── get-kakao-token.js     # 카카오 토큰 발급 스크립트
│   ├── send-kakao.js          # 카카오톡 전송 스크립트
│   └── package.json           # Node.js 의존성
└── .env                        # API 키 저장 (gitignore 필요)
```

## 실행 로직 (Claude가 수행)

이 스킬이 트리거되면 다음 순서로 실행합니다:

1. **네이버 경제 뉴스 수집**
   - WebFetch로 네이버 뉴스 RSS 읽기
   - 최신 10개 기사 파싱

2. **주요 소식 요약**
   - 기사 제목과 요약을 분석
   - 핵심 3-5개 뉴스 선별
   - 읽기 쉬운 형태로 재구성

3. **임시 파일 저장**
   - 요약 결과를 `.claude/skills/morning-briefing/latest-briefing.md`에 저장

4. **카카오톡 전송**
   - Node.js 스크립트 실행
   - 요약 내용을 카카오톡으로 전송

5. **결과 리포트**
   - 수집된 뉴스 개수
   - 전송 성공/실패 여부
   - 저장된 파일 경로

## 커스터마이징

### 뉴스 소스 변경

다른 뉴스 소스를 추가하려면 `SKILL.md`의 "Step 1" 섹션을 수정하세요:

```markdown
### 추가 소스: 한국경제

- URL: `https://www.hankyung.com/feed/economy`
- RSS: 한경 경제 피드
```

### 요약 스타일 변경

"Step 2"의 요약 기준을 수정하여 원하는 스타일로 변경하세요:

```markdown
요약 기준:
- 각 뉴스를 1줄로 초간단 요약
- 이모지 사용
- 중요도 표시 (⭐⭐⭐)
```

### 전송 시간 변경

crontab의 시간을 수정하세요:

```bash
# 오전 7시로 변경
0 7 * * * cd /Users/sol/Desktop/sol/claude && claude skill run morning-briefing

# 오전 9시, 오후 6시 2회 실행
0 9,18 * * * cd /Users/sol/Desktop/sol/claude && claude skill run morning-briefing
```

## 트러블슈팅

### 카카오 토큰 만료

Access Token은 보통 6시간 후 만료됩니다. Refresh Token을 사용하여 자동 갱신하거나, 장기 토큰을 발급받으세요.

```bash
# 토큰 재발급
node .claude/skills/morning-briefing/scripts/get-kakao-token.js
```

### 뉴스 수집 실패

네이버 RSS가 변경되었을 수 있습니다. RSS URL을 확인하고 업데이트하세요.

### cron 실행 안됨

cron 로그를 확인하세요:

```bash
# cron 로그 확인 (Mac)
log show --predicate 'eventMessage contains "cron"' --last 1h
```

Claude Code 경로가 올바른지 확인:

```bash
which claude
```

## 참고 자료

- [카카오 개발자 문서 - 나에게 보내기](https://developers.kakao.com/docs/latest/ko/message/rest-api)
- [네이버 뉴스 RSS](https://news.naver.com/main/list.naver?mode=LPOD&mid=sec&sid1=101)
- [Mac crontab 가이드](https://support.apple.com/ko-kr/guide/terminal/apdc6c1077b-5d5d-4d35-9c19-60f2397b2369/mac)
